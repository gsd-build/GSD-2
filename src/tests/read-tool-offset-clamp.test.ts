/**
 * Read tool offset clamping tests (#3007).
 *
 * Tests that directly exercised pi-coding-agent's createReadTool() were removed
 * per D-12 (GSD tests must only test GSD code, not pi-* vendor packages).
 * pi-coding-agent has its own test suite for read tool behavior.
 */
