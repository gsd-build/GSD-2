---
phase: 13-session-streaming-hardening
plan: "06"
type: execute
wave: 3
depends_on:
  - 13-03
  - 13-04
  - 13-05
files_modified:
  - packages/mission-control/src/app/AppShell.tsx
  - packages/mission-control/src/server/server.ts
autonomous: false
requirements:
  - STREAM-03
  - STREAM-04
  - STREAM-05
  - STREAM-06
  - STREAM-07
must_haves:
  truths:
    - "All Phase 13 props flow from the root: isAutoMode, isCrashed, costState, onInterrupt wired from useSessionManager down to ChatView"
    - "SessionManager.killAll() is called when the Bun server process receives SIGTERM/SIGINT — no orphaned gsd processes remain after app restart"
    - "useCostTracker's budgetCeiling is read from preferences.md via settings-api and passed into useSessionManager"
    - "A running gsd session streams all 8 Pi SDK event types with distinct rendering — verified manually by the user"
  artifacts:
    - path: "packages/mission-control/src/app/AppShell.tsx"
      provides: "isAutoMode, isCrashed, costState, onInterrupt wired to ChatView"
    - path: "packages/mission-control/src/server/server.ts"
      provides: "SIGTERM/SIGINT handler calls sessionManager.killAll()"
  key_links:
    - from: "AppShell.tsx"
      to: "useSessionManager"
      via: "destructures isAutoMode, isCrashed, costState, interrupt"
      pattern: "isAutoMode"
    - from: "server.ts"
      to: "SessionManager.killAll"
      via: "process.on('SIGTERM') and process.on('SIGINT')"
      pattern: "killAll"
---

<objective>
Wire all Phase 13 state down from hooks to components, register the killAll orphan guard on the server, and confirm everything works end-to-end with a human verification checkpoint.

Purpose: Plans 13-01 through 13-05 built types, hooks, and components. This plan connects them so the running app surfaces all Phase 13 features. It also closes the orphan prevention loop by calling killAll() on server shutdown.

Output:
- AppShell passes isAutoMode, isCrashed, costState, onInterrupt to ChatView
- server.ts registers SIGTERM/SIGINT handlers calling sessionManager.killAll()
- Human verifies all 5 success criteria with a live gsd session
</objective>

<execution_context>
@C:/Users/Bantu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bantu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-session-streaming-hardening/13-CONTEXT.md
@.planning/phases/13-session-streaming-hardening/13-03-SUMMARY.md
@.planning/phases/13-session-streaming-hardening/13-04-SUMMARY.md
@.planning/phases/13-session-streaming-hardening/13-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 13 props through AppShell and register killAll on shutdown</name>
  <files>
    packages/mission-control/src/app/AppShell.tsx,
    packages/mission-control/src/server/server.ts
  </files>
  <action>
**Part A — AppShell.tsx wiring:**

Open `src/app/AppShell.tsx`. It uses `useSessionManager` (or passes props to `SingleColumnView` → `ChatView`). Destructure the new return values from `useSessionManager`:

```typescript
const {
  sessions,
  activeSessionId,
  // ... existing destructuring ...
  isAutoMode,
  isCrashed,
  costState,
  interrupt,
} = useSessionManager({ budgetCeiling: settings?.budgetCeiling ?? null });
```

If `budgetCeiling` is not yet in settings, read it as `settings?.budget_ceiling` (the field name from `preferences.md` — check `settings-api.ts` to confirm the exact field name). Pass `null` if not set.

Pass the new props down to `ChatView` (via `SingleColumnView` → `ChatViewConnected` or directly):

```tsx
<ChatView
  {...existingProps}
  isAutoMode={isAutoMode}
  isCrashed={isCrashed}
  costState={costState}
  onInterrupt={interrupt}
  onDismissCrash={() => {/* reset isCrashed in useSessionManager */}}
/>
```

For `onDismissCrash`: add a `resetCrash` function to `useSessionManager` that calls `setIsCrashed(false)`.

**Part B — server.ts orphan prevention:**

Open `src/server/server.ts`. Find where the server is initialized. Add shutdown handlers after the server starts:

```typescript
const cleanup = async () => {
  console.log("[server] Shutting down — killing all gsd processes...");
  await sessionManager.killAll();
  wsServer.stop();
  process.exit(0);
};

process.on("SIGTERM", cleanup);
process.on("SIGINT", cleanup);
```

Ensure `sessionManager` is in scope (it should already be used in server.ts for session routing). If the cleanup handler is already present, extend it to call `killAll()`.
  </action>
  <verify>
    <automated>cd packages/mission-control && bun tsc --noEmit</automated>
  </verify>
  <done>AppShell passes all Phase 13 props to ChatView; server.ts has SIGTERM/SIGINT cleanup calling killAll(); TypeScript compiles cleanly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All Phase 13 features are now wired end-to-end:
1. Pi SDK event classifier (plan 13-01) — 8 event types with typed discrimination
2. Process lifecycle hardening (plan 13-02) — interrupt(), crash events, killAll()
3. WebSocket reconnect with refresh-on-reconnect + crash banner (plan 13-03)
4. Cost badge and budget warnings in chat header (plan 13-04)
5. EXECUTING badge, phase transition cards, tool cards, Escape interrupt (plan 13-05)
6. Full wiring and orphan prevention (plan 13-06, task 1)
  </what-built>
  <how-to-verify>
Start the dev server:
```
cd packages/mission-control && bun dev
```
Open http://localhost:4000 in a browser.

**SC-1: Streaming event types**
Open a GSD 2 project and send `/gsd auto` in chat.
Expected: phase transition divider cards appear (Research / Planning / Executing), tool use cards show tool name with spinner, text messages appear as normal chat bubbles.

**SC-2: Resilience to malformed input**
No direct action — confirmed by plan 13-01 tests. Verify the test suite passes:
```
cd packages/mission-control && bun test tests/pi-sdk-classifier.test.ts
```

**SC-3: WebSocket reconnect**
Open the app, start a gsd session, then close and reopen the browser tab within 30 seconds.
Expected: the app reconnects (status goes connecting → connected), chat history and task panel are restored from .gsd/ files.

**SC-4: Cost badge**
While a session is running, observe the chat header.
Expected: a small monospace badge (e.g. `$0.04`) appears in cyan. If your test project has a budget_ceiling set in .gsd/preferences.md, set it low enough to trigger the amber warning at 80% and red critical banner at 95%.

**SC-5: EXECUTING badge and Escape**
While `/gsd auto` is running, observe the chat header.
Expected: amber "EXECUTING" badge with pulsing dot.
Press Escape.
Expected: the gsd process receives SIGINT, auto mode stops, badge disappears. Chat history is preserved.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 success criteria pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
```bash
cd packages/mission-control && bun test
cd packages/mission-control && bun tsc --noEmit
```
Full test suite passes, zero TypeScript errors.
</verification>

<success_criteria>
- All Phase 13 props wired from useSessionManager through AppShell to ChatView
- SIGTERM/SIGINT call killAll() — no orphaned gsd processes
- Human verifies all 5 streaming success criteria with a live gsd session
- Full test suite green (bun test)
- Zero TypeScript errors (bun tsc --noEmit)
</success_criteria>

<output>
After completion, create `.planning/phases/13-session-streaming-hardening/13-06-SUMMARY.md`
</output>
