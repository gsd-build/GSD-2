---
phase: 08-breaking-api-migrations
plan: 03
type: execute
wave: 2
depends_on: [08-02]
files_modified:
  - packages/gsd-agent-core/src/agent-session.ts
autonomous: true
requirements: [SESS-01]

must_haves:
  truths:
    - "All session_switch and session_fork emissions replaced with session_start + reason"
    - "No references to session_switch or session_fork remain in GSD packages"
  artifacts:
    - path: "packages/gsd-agent-core/src/agent-session.ts"
      provides: "session_start emissions with reason: new, resume, fork"
      contains: "session_start"
  key_links:
    - from: "packages/gsd-agent-core/src/agent-session.ts"
      to: "packages/pi-coding-agent/src/core/extensions/types.ts"
      via: "SessionStartEvent type shape"
      pattern: "type.*session_start.*reason"
---

<objective>
Migrate all three session event emission sites in agent-session.ts from session_switch/session_fork to session_start with the correct reason values.

Purpose: Satisfy SESS-01 — the session event contract must use the 0.67.2 `SessionStartEvent` type with `reason` field.
Output: agent-session.ts emits `session_start` with `reason: "new"`, `reason: "resume"`, and `reason: "fork"` at the three emission sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-breaking-api-migrations/08-CONTEXT.md
@.planning/phases/08-breaking-api-migrations/08-RESEARCH.md
@.planning/phases/08-breaking-api-migrations/08-PATTERNS.md
@.planning/session-migration-trace.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate session_switch and session_fork emissions to session_start</name>
  <files>packages/gsd-agent-core/src/agent-session.ts</files>
  <read_first>
    - .planning/session-migration-trace.md (reason mappings — read first per D-07 requirement)
    - packages/gsd-agent-core/src/agent-session.ts (lines 1600-1620 for emission site 1 "new", lines 2425-2445 for emission site 2 "resume", lines 2525-2545 for emission site 3 "fork")
    - packages/pi-coding-agent/src/core/extensions/types.ts (SessionStartEvent at line ~445 — confirm reason union type)
  </read_first>
  <action>
Per D-05 and D-06, with mappings from the rubber-duck trace doc:

**Site 1 — newSession path (lines ~1610-1617):**
- BEFORE: `await this._extensionRunner.emit({ type: "session_switch", reason: "new", previousSessionFile });`
- AFTER: `await this._extensionRunner.emit({ type: "session_start", reason: "new", previousSessionFile });`
- Change ONLY `type: "session_switch"` to `type: "session_start"` — the `reason: "new"` value stays the same

**Site 2 — resume/switch path (lines ~2433-2440):**
- BEFORE: `await this._extensionRunner.emit({ type: "session_switch", reason: "resume", previousSessionFile });`
- AFTER: `await this._extensionRunner.emit({ type: "session_start", reason: "resume", previousSessionFile });`
- Change ONLY `type: "session_switch"` to `type: "session_start"` — the `reason: "resume"` value stays the same

**Site 3 — fork path (lines ~2534-2540):**
- BEFORE: `await this._extensionRunner.emit({ type: "session_fork", previousSessionFile });`
- AFTER: `await this._extensionRunner.emit({ type: "session_start", reason: "fork", previousSessionFile });`
- Change `type: "session_fork"` to `type: "session_start"` AND ADD `reason: "fork"` (the old session_fork event had no reason field)

After editing, run a full file grep to confirm zero remaining references:
`grep -n "session_switch\|session_fork" packages/gsd-agent-core/src/agent-session.ts`
This should return zero matches. If any remain (comments, string literals, variable names), update them too.

Also run the cross-package grep:
`grep -r "session_switch\|session_fork" packages/pi-coding-agent/src/ packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/`
The RESEARCH.md confirms the only emission sites are in agent-session.ts. Handler callsites (`register-hooks.ts:89`, `bg-shell-lifecycle.ts:97/396`, `mcp-client/index.ts:517`) referenced in CONTEXT.md D-06 are in runtime extension resources, not compiled TypeScript — they produce no type errors and are behavioral-only. If the cross-package grep returns matches in those runtime files, note them but they are out of scope for the compile gate.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && grep -c "session_switch\|session_fork" packages/gsd-agent-core/src/agent-session.ts; echo "---"; grep -n "session_start" packages/gsd-agent-core/src/agent-session.ts | head -10; echo "---"; grep -r "session_switch\|session_fork" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/ | grep -v "node_modules" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "session_switch" packages/gsd-agent-core/src/agent-session.ts` returns `0`
    - `grep -c "session_fork" packages/gsd-agent-core/src/agent-session.ts` returns `0`
    - `grep "session_start" packages/gsd-agent-core/src/agent-session.ts` returns at least 3 matches (the three emission sites)
    - `grep 'reason: "new"' packages/gsd-agent-core/src/agent-session.ts` returns at least 1 match
    - `grep 'reason: "resume"' packages/gsd-agent-core/src/agent-session.ts` returns at least 1 match
    - `grep 'reason: "fork"' packages/gsd-agent-core/src/agent-session.ts` returns at least 1 match
    - `grep -r "session_switch\|session_fork" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` returns zero matches
  </acceptance_criteria>
  <done>All three session event emission sites migrated: session_switch(new) -> session_start(new), session_switch(resume) -> session_start(resume), session_fork -> session_start(fork). Zero remaining session_switch/session_fork references in gsd-agent-core and gsd-agent-modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extension event emission | Session events emitted to extension runner — consumed by extension handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Repudiation | session_start event reason values | accept | The reason values ("new", "resume", "fork") are hardcoded string literals at the emission sites — they cannot be influenced by user input. Extension handlers receive the same trust level as before. |
</threat_model>

<verification>
`grep -r "session_switch\|session_fork" packages/pi-coding-agent/src/ packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` returns zero matches (Phase 08 success criterion 1).
</verification>

<success_criteria>
- Zero `session_switch` references in all three GSD packages
- Zero `session_fork` references in all three GSD packages
- Three `session_start` emissions in agent-session.ts with reasons "new", "resume", "fork"
</success_criteria>

<output>
After completion, create `.planning/phases/08-breaking-api-migrations/08-03-SUMMARY.md`
</output>
