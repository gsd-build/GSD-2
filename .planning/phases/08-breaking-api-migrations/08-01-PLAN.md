---
phase: 08-breaking-api-migrations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pi-coding-agent/src/core/extensions/index.ts
  - packages/gsd-agent-core/src/lifecycle-hook-types.ts
  - packages/gsd-agent-core/src/lifecycle-hooks.ts
  - packages/pi-coding-agent/src/resources/extensions/memory/index.ts
  - packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts
autonomous: true
requirements: [SESS-01, MREG-01]

must_haves:
  truths:
    - "All 24 Phase 07 type errors in pi-coding-agent are resolved"
    - "LifecycleHook types are defined locally in gsd-agent-core for Phase 08 use"
    - "lifecycle-hooks.ts compiles without importExtensionModule or extension.lifecycleHooks"
    - "provider-manager.ts uses full keybinding IDs and no private modelsJsonPath access"
  artifacts:
    - path: "packages/pi-coding-agent/src/core/extensions/index.ts"
      provides: "Clean barrel re-exports — 17 removed types gone"
    - path: "packages/gsd-agent-core/src/lifecycle-hook-types.ts"
      provides: "Local LifecycleHook type definitions"
      exports: ["LifecycleHookPhase", "LifecycleHookScope", "LifecycleHookContext", "LifecycleHookHandler", "LifecycleHookMap"]
    - path: "packages/gsd-agent-core/src/lifecycle-hooks.ts"
      provides: "Updated imports from local types, no importExtensionModule, no extension.lifecycleHooks"
    - path: "packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts"
      provides: "Fixed keybinding IDs and modelsJsonPath workaround"
    - path: "packages/pi-coding-agent/src/resources/extensions/memory/index.ts"
      provides: "getRetrySettings() call replacing getMemorySettings()"
  key_links:
    - from: "packages/gsd-agent-core/src/lifecycle-hooks.ts"
      to: "packages/gsd-agent-core/src/lifecycle-hook-types.ts"
      via: "import type { LifecycleHookContext, ... } from './lifecycle-hook-types.js'"
      pattern: "from.*lifecycle-hook-types"
---

<objective>
Fix all 24 Phase 07 type errors in pi-coding-agent and proactively fix the hidden lifecycle-hooks compile errors that will surface after pi-coding-agent rebuild.

Purpose: Unblock `pi-coding-agent` rebuild and prevent cascading failures in `gsd-agent-core` when the stale dist is replaced.
Output: All 17 broken re-exports removed, lifecycle hook types defined locally, provider-manager keybinding and privacy fixes applied, memory settings method renamed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-vendor-swap/type-errors.md
@.planning/phases/08-breaking-api-migrations/08-CONTEXT.md
@.planning/phases/08-breaking-api-migrations/08-RESEARCH.md
@.planning/phases/08-breaking-api-migrations/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 17 broken re-exports from extensions/index.ts and fix memory/index.ts</name>
  <files>packages/pi-coding-agent/src/core/extensions/index.ts, packages/pi-coding-agent/src/resources/extensions/memory/index.ts</files>
  <read_first>
    - packages/pi-coding-agent/src/core/extensions/index.ts (current barrel file with broken re-exports)
    - packages/pi-coding-agent/src/core/extensions/types.ts (source of truth — confirms which types exist in 0.67.2)
    - packages/pi-coding-agent/src/resources/extensions/memory/index.ts (getMemorySettings call site)
  </read_first>
  <action>
Per D-01 and D-03: First run `grep -rn "SessionForkEvent\|SessionSwitchEvent\|SessionDirectoryEvent\|SessionDirectoryHandler\|SessionDirectoryResult\|LifecycleHookContext\|LifecycleHookHandler\|LifecycleHookMap\|LifecycleHookPhase\|LifecycleHookScope\|AppAction\|AdjustToolSetEvent\|AdjustToolSetResult\|ToolCompatibility\|BashTransformEvent\|BashTransformEventResult\|isToolResultEventType" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` to confirm all callers. Expected: only `lifecycle-hooks.ts` imports the LifecycleHook types; zero callers for the rest.

In `extensions/index.ts`:
- Remove these 17 type names from the `export type { ... } from "./types.js"` block: `AppAction`, `AdjustToolSetEvent`, `AdjustToolSetResult`, `LifecycleHookContext`, `LifecycleHookHandler`, `LifecycleHookMap`, `LifecycleHookPhase`, `LifecycleHookScope`, `SessionDirectoryEvent`, `SessionDirectoryHandler`, `SessionDirectoryResult`, `SessionForkEvent`, `SessionSwitchEvent`, `ToolCompatibility`, `BashTransformEvent`, `BashTransformEventResult`
- Replace the line `export { isToolCallEventType, isToolResultEventType } from "./types.js";` with `export { isToolCallEventType } from "./types.js";` (remove `isToolResultEventType` — does not exist in types.ts)
- Keep ALL other exports intact

In `memory/index.ts` at line 60 per D-16:
- Replace `memorySettings = sm.getMemorySettings();` with `memorySettings = sm.getRetrySettings();`
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && grep -c "SessionForkEvent\|SessionSwitchEvent\|SessionDirectoryEvent\|SessionDirectoryHandler\|SessionDirectoryResult\|LifecycleHookContext\|LifecycleHookHandler\|LifecycleHookMap\|LifecycleHookPhase\|LifecycleHookScope\|AppAction\|AdjustToolSetEvent\|AdjustToolSetResult\|ToolCompatibility\|BashTransformEvent\|BashTransformEventResult\|isToolResultEventType" packages/pi-coding-agent/src/core/extensions/index.ts; echo "---"; grep "getMemorySettings" packages/pi-coding-agent/src/resources/extensions/memory/index.ts; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c` of all 17 removed type names in `extensions/index.ts` returns `0`
    - `grep "isToolResultEventType" packages/pi-coding-agent/src/core/extensions/index.ts` returns zero matches
    - `grep "isToolCallEventType" packages/pi-coding-agent/src/core/extensions/index.ts` returns one match (retained)
    - `grep "getMemorySettings" packages/pi-coding-agent/src/resources/extensions/memory/index.ts` returns zero matches
    - `grep "getRetrySettings" packages/pi-coding-agent/src/resources/extensions/memory/index.ts` returns one match
  </acceptance_criteria>
  <done>All 17 broken re-exports removed from extensions/index.ts, isToolResultEventType removed, getMemorySettings replaced with getRetrySettings</done>
</task>

<task type="auto">
  <name>Task 2: Create lifecycle-hook-types.ts and fix lifecycle-hooks.ts imports</name>
  <files>packages/gsd-agent-core/src/lifecycle-hook-types.ts, packages/gsd-agent-core/src/lifecycle-hooks.ts</files>
  <read_first>
    - packages/gsd-agent-core/src/lifecycle-hooks.ts (full file — import block, extension.lifecycleHooks at line 162, importExtensionModule at line 206, context construction at lines 236-246)
    - packages/pi-coding-agent/src/core/extensions/types.ts (Extension interface at line ~1421 — confirm handlers field shape)
  </read_first>
  <action>
Per D-02 (corrected by RESEARCH.md — types do NOT exist in pi-coding-agent 0.67.2 source):

Create NEW file `packages/gsd-agent-core/src/lifecycle-hook-types.ts` with these exact contents:
```typescript
// Temporary home for LifecycleHook* types removed from @gsd/pi-coding-agent 0.67.2.
// Phase 09 moves these to @gsd/agent-types.

export type LifecycleHookPhase = string;
export type LifecycleHookScope = "project" | "user";

export interface LifecycleHookContext {
  phase: LifecycleHookPhase;
  source: string;
  installedPath?: string;
  scope: LifecycleHookScope;
  cwd: string;
  interactive: boolean;
  log: (message: string) => void;
  warn: (message: string) => void;
  error: (message: string) => void;
}

export type LifecycleHookHandler = (context: LifecycleHookContext) => void | Promise<void>;
export type LifecycleHookMap = Partial<Record<LifecycleHookPhase, LifecycleHookHandler[]>>;
```

In `lifecycle-hooks.ts`, replace the import block (lines 1-16):
- BEFORE: imports `importExtensionModule`, `loadExtensions`, and 5 LifecycleHook types all from `"@gsd/pi-coding-agent"`
- AFTER: `import { loadExtensions } from "@gsd/pi-coding-agent";` on one line, then `import type { LifecycleHookContext, LifecycleHookHandler, LifecycleHookMap, LifecycleHookPhase, LifecycleHookScope } from "./lifecycle-hook-types.js";` on another

At line 162, replace `extension.lifecycleHooks` access:
- BEFORE: `hooksByPath.set(extension.path, extension.lifecycleHooks);`
- AFTER: `hooksByPath.set(extension.path, Object.fromEntries(extension.handlers) as LifecycleHookMap);`

At line 206, replace `importExtensionModule`:
- BEFORE: `module = await importExtensionModule<Record<string, unknown>>(import.meta.url, pathToFileURL(entryPath).href);`
- AFTER: `module = await import(pathToFileURL(entryPath).href) as Record<string, unknown>;`

Verify the `pathToFileURL` import exists at the top of the file (from `node:url`). If not, add it.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && test -f packages/gsd-agent-core/src/lifecycle-hook-types.ts && grep "LifecycleHookContext" packages/gsd-agent-core/src/lifecycle-hook-types.ts && grep "lifecycle-hook-types" packages/gsd-agent-core/src/lifecycle-hooks.ts && grep -c "importExtensionModule" packages/gsd-agent-core/src/lifecycle-hooks.ts && grep -c "extension.lifecycleHooks" packages/gsd-agent-core/src/lifecycle-hooks.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/gsd-agent-core/src/lifecycle-hook-types.ts` exists and exports `LifecycleHookContext`, `LifecycleHookHandler`, `LifecycleHookMap`, `LifecycleHookPhase`, `LifecycleHookScope`
    - `grep "lifecycle-hook-types" packages/gsd-agent-core/src/lifecycle-hooks.ts` returns at least one match (import present)
    - `grep -c "importExtensionModule" packages/gsd-agent-core/src/lifecycle-hooks.ts` returns `0`
    - `grep -c "extension.lifecycleHooks" packages/gsd-agent-core/src/lifecycle-hooks.ts` returns `0`
    - `grep "Object.fromEntries(extension.handlers)" packages/gsd-agent-core/src/lifecycle-hooks.ts` returns one match
    - `grep "await import(" packages/gsd-agent-core/src/lifecycle-hooks.ts` returns at least one match
  </acceptance_criteria>
  <done>LifecycleHook types defined locally in gsd-agent-core, lifecycle-hooks.ts imports updated, importExtensionModule replaced with dynamic import, extension.lifecycleHooks replaced with extension.handlers cast</done>
</task>

<task type="auto">
  <name>Task 3: Fix provider-manager.ts keybinding IDs and modelsJsonPath privacy</name>
  <files>packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts</files>
  <read_first>
    - packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts (full file — lines 21-24 for ModelsJsonWriter stub, line 64-69 for modelsJsonPath access, lines 170-216 for keybinding matches calls)
    - packages/pi-coding-agent/src/core/keybindings.ts (keybinding ID mapping — lines 162-167 for selectUp/Down/Cancel/Confirm → tui.select.*)
  </read_first>
  <action>
Per D-11 and PATTERNS.md:

**Fix 1 — modelsJsonPath private access (line ~64-69):**
The `ModelsJsonWriter` is already stubbed at lines 21-24 as a no-op. Replace the constructor call:
- BEFORE: `this.modelsJsonWriter = new ModelsJsonWriter(this.modelRegistry.modelsJsonPath);`
- AFTER: `this.modelsJsonWriter = new ModelsJsonWriter("");`
This works because the stub's `removeProvider()` at line ~199 is already a no-op — it never reads the path.

If the `modelsJsonPath` type is `string | undefined` causing TS2345, the empty string resolves that too.

**Fix 2 — keybinding shorthand → full IDs (4 sites):**
- Line ~170 (or 175 per type-errors.md): `kb.matches(keyData, "selectUp")` → `kb.matches(keyData, "tui.select.up")`
- Line ~175 (or 180): `kb.matches(keyData, "selectDown")` → `kb.matches(keyData, "tui.select.down")`
- Line ~180 (or 185): `kb.matches(keyData, "selectCancel")` → `kb.matches(keyData, "tui.select.cancel")`
- Line ~211 (or 216): `kb.matches(keyData, "selectConfirm")` → `kb.matches(keyData, "tui.select.confirm")`
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && grep -n "selectUp\|selectDown\|selectCancel\|selectConfirm" packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts | grep -v "tui.select"; echo "exit:$?"; grep "modelsJsonPath" packages/pi-coding-agent/src/modes/interactive/components/provider-manager.ts | grep -v "ModelsJsonWriter"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "selectUp" provider-manager.ts` only appears inside `"tui.select.up"` string (no bare `"selectUp"`)
    - `grep "selectDown" provider-manager.ts` only appears inside `"tui.select.down"` string
    - `grep "selectCancel" provider-manager.ts` only appears inside `"tui.select.cancel"` string
    - `grep "selectConfirm" provider-manager.ts` only appears inside `"tui.select.confirm"` string
    - `grep "this.modelRegistry.modelsJsonPath" provider-manager.ts` returns zero matches
    - `grep 'ModelsJsonWriter("")' provider-manager.ts` or similar shows the constructor uses empty string or no modelRegistry access
  </acceptance_criteria>
  <done>All 7 type errors in provider-manager.ts resolved — keybinding IDs use full dotted format, modelsJsonPath private access removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Extension module loading | `lifecycle-hooks.ts` uses `import()` to load user-authored extension modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | lifecycle-hooks.ts dynamic import | accept | Pre-existing behavior — `importExtensionModule` was also a dynamic import wrapper. Extension paths come from the extension manifest loaded by `loadExtensions()`, same trust chain as before. No new attack surface. |
| T-08-02 | Spoofing | extension.handlers cast to LifecycleHookMap | accept | The cast is type-level only — runtime data comes from the extension system's `Map<string, HandlerFn[]>` which is populated by pi-coding-agent's validated loader. No user input at this boundary. |
</threat_model>

<verification>
After all 3 tasks: run `npm run build:pi 2>&1 | tail -30` — expect zero type errors from the 24 catalogued in Phase 07. Some new errors may surface from lifecycle-hooks.ts changes if the `extension.handlers` cast shape is wrong — fix in-task.
</verification>

<success_criteria>
- All 24 type errors from Phase 07 catalogue resolved
- `grep -c "SessionForkEvent\|SessionSwitchEvent" packages/pi-coding-agent/src/core/extensions/index.ts` returns 0
- `lifecycle-hook-types.ts` exists with 5 exported types
- `lifecycle-hooks.ts` has no imports of `importExtensionModule` or LifecycleHook types from `@gsd/pi-coding-agent`
- `provider-manager.ts` uses `tui.select.*` keybinding IDs
- `memory/index.ts` calls `getRetrySettings()` not `getMemorySettings()`
</success_criteria>

<output>
After completion, create `.planning/phases/08-breaking-api-migrations/08-01-SUMMARY.md`
</output>
