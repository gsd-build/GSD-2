---
phase: 08-breaking-api-migrations
plan: 05
type: execute
wave: 4
depends_on: [08-01, 08-02, 08-03, 08-04]
files_modified:
  - packages/gsd-agent-modes/src/modes/interactive/components/tool-execution.ts
autonomous: true
requirements: [TOOL-01, TOOL-02]

must_haves:
  truths:
    - "TOOL-01: edit tool callers handle edits[] array format for pre-execution diff preview"
    - "TOOL-02: confirmed no GSD-owned ToolDefinition needs prepareArguments hook"
    - "tsc --noEmit on gsd-agent-core and gsd-agent-modes exits 0"
    - "Phase 08 compile gate passes"
  artifacts:
    - path: "packages/gsd-agent-modes/src/modes/interactive/components/tool-execution.ts"
      provides: "maybeComputeEditDiff handles edits[] format"
  key_links:
    - from: "packages/gsd-agent-modes/src/modes/interactive/components/tool-execution.ts"
      to: "packages/pi-coding-agent/src/core/tools/edit-diff.ts"
      via: "computeEditDiff or computeEditsDiff call"
      pattern: "computeEdit"
---

<objective>
Satisfy TOOL-01 by updating maybeComputeEditDiff to handle the edits[] array format, confirm TOOL-02 is already satisfied, and run the Phase 08 compile gate.

Purpose: Complete all Phase 08 requirements and verify the compile gate passes.
Output: tool-execution.ts handles edits[] format; TOOL-02 confirmed; `tsc --noEmit` on gsd-agent-core and gsd-agent-modes exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-breaking-api-migrations/08-CONTEXT.md
@.planning/phases/08-breaking-api-migrations/08-RESEARCH.md
@.planning/phases/08-breaking-api-migrations/08-PATTERNS.md
@.planning/phases/08-breaking-api-migrations/08-01-SUMMARY.md
@.planning/phases/08-breaking-api-migrations/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update maybeComputeEditDiff for edits[] format and confirm TOOL-02</name>
  <files>packages/gsd-agent-modes/src/modes/interactive/components/tool-execution.ts</files>
  <read_first>
    - packages/gsd-agent-modes/src/modes/interactive/components/tool-execution.ts (lines 843-878 — maybeComputeEditDiff function, understand current args.oldText + args.newText check)
    - packages/pi-coding-agent/src/core/tools/edit-diff.ts (line ~438 — computeEditDiff wrapper, and the multi-edit computeEditsDiff function — understand both signatures)
  </read_first>
  <action>
Per TOOL-01 (RESEARCH.md clarification — not a compile error but behavioral correctness):

**TOOL-02 confirmation first:** Run `grep -rn "prepareArguments" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` to confirm zero GSD-owned ToolDefinition objects use or need `prepareArguments`. If zero matches, TOOL-02 is satisfied — no GSD code needs the hook.

**TOOL-01 — maybeComputeEditDiff update:**
The current `maybeComputeEditDiff()` (around line 843) only handles the single-edit format:
```typescript
if (args.oldText && args.newText) {
  // compute diff for single edit
}
```

Update to also handle the `edits[]` array format. Read the `computeEditsDiff` function from `edit-diff.ts` to understand its signature. Then add an `else if` branch:

```typescript
if (args.oldText && args.newText) {
  // existing single-edit path — keep as-is
} else if (args.edits && Array.isArray(args.edits)) {
  // New multi-edit path: iterate edits[], compute diff for each
  for (const edit of args.edits) {
    if (edit.oldText && edit.newText) {
      // Use computeEditDiff(path, edit.oldText, edit.newText, cwd)
      // or computeEditsDiff if available for batch
    }
  }
}
```

The exact implementation depends on what `computeEditsDiff` accepts. Read `edit-diff.ts` to determine whether it takes a single `edits[]` array or individual pairs. Adapt the call pattern accordingly.

If `computeEditsDiff` takes `(path, edits, cwd)` where `edits` is `{oldText, newText}[]`, the call is:
```typescript
const diff = computeEditsDiff(args.file_path, args.edits, cwd);
```

**Compile gate (after both fixes):**
Run `npm run build:pi 2>&1 | tail -30` to rebuild pi-coding-agent with the Plan 01 fixes.
Then run `cd packages/gsd-agent-core && npx tsc --noEmit 2>&1 | tail -20` and `cd packages/gsd-agent-modes && npx tsc --noEmit 2>&1 | tail -20`.

If circular dep errors appear from pi-coding-agent imports, those are acceptable per the phase success criteria (Phase 09 resolves them). The gate is: zero errors in gsd-agent-core and gsd-agent-modes excluding circular dep errors from pi-coding-agent.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && grep "edits" packages/gsd-agent-modes/src/modes/interactive/components/tool-execution.ts | head -5; echo "---"; grep -rn "prepareArguments" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/ | head -5; echo "---"; npm run build:pi 2>&1 | tail -10; echo "---compile-gate---"; cd packages/gsd-agent-core && npx tsc --noEmit 2>&1 | tail -10; cd /Users/trekkie/projects/gsd-2/packages/gsd-agent-modes && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "args.edits" packages/gsd-agent-modes/src/modes/interactive/components/tool-execution.ts` returns at least one match (edits[] handling added)
    - `grep -rn "prepareArguments" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` returns zero matches OR only shows imports/type references (no missing implementation — TOOL-02 confirmed)
    - `npm run build:pi` completes without crashing
    - `tsc --noEmit` on gsd-agent-core exits 0 (or only shows pi-coding-agent circular dep errors)
    - `tsc --noEmit` on gsd-agent-modes exits 0 (or only shows pi-coding-agent circular dep errors)
  </acceptance_criteria>
  <done>TOOL-01 satisfied — maybeComputeEditDiff handles edits[] format. TOOL-02 confirmed — no GSD code needs prepareArguments. Compile gate passes on gsd-agent-core and gsd-agent-modes.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Phase 08 verification gates</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md (Phase 08 success criteria — 5 items to verify)
    - .planning/session-migration-trace.md (confirm existence for SC-5)
  </read_first>
  <action>
Run all 5 Phase 08 success criteria checks from ROADMAP.md:

1. `grep -r "session_switch\|session_fork" packages/pi-coding-agent/src/ packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` — must return zero matches
2. `grep -r "new ModelRegistry" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` — must return zero matches
3. `grep -r "session_directory" packages/pi-coding-agent/src/ packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` — must return zero matches
4. `tsc --noEmit` on gsd-agent-core and gsd-agent-modes — must exit 0 (pi-coding-agent circular dep errors acceptable)
5. `ls .planning/session-migration-trace.md` — must exist

If any gate fails, investigate and fix. Do NOT mark as done with failing gates.

Also run the baseline test check: `npm run test:unit 2>&1 | tail -5` — count failures. Phase 08 must not add new failures above the 258 pre-existing baseline noted in VALIDATION.md.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && echo "SC-1:"; grep -r "session_switch\|session_fork" packages/pi-coding-agent/src/ packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/ | wc -l; echo "SC-2:"; grep -r "new ModelRegistry" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/ | wc -l; echo "SC-3:"; grep -r "session_directory" packages/pi-coding-agent/src/ packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/ | wc -l; echo "SC-5:"; ls .planning/session-migration-trace.md 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - SC-1: `grep session_switch|session_fork` across all 3 packages returns 0 lines
    - SC-2: `grep "new ModelRegistry"` in gsd-agent-core and gsd-agent-modes returns 0 lines
    - SC-3: `grep "session_directory"` across all 3 packages returns 0 lines
    - SC-4: `tsc --noEmit` on gsd-agent-core and gsd-agent-modes exits 0 (pi-coding-agent circular errors acceptable)
    - SC-5: `.planning/session-migration-trace.md` exists
    - Test baseline: failure count not higher than 258 pre-existing
  </acceptance_criteria>
  <done>All 5 Phase 08 success criteria verified. Compile gate passes. No new test failures above baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool argument parsing | `args.edits` comes from LLM output — untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering | maybeComputeEditDiff args.edits | mitigate | Validate `Array.isArray(args.edits)` before iterating. Each edit element must have `oldText` and `newText` string properties — check before passing to `computeEditDiff`. This matches the existing validation pattern for `args.oldText`/`args.newText`. |
</threat_model>

<verification>
All 5 Phase 08 ROADMAP success criteria pass. Compile gate green. No new test failures.
</verification>

<success_criteria>
- All Phase 08 ROADMAP success criteria (5/5) verified
- TOOL-01: edits[] format handled in maybeComputeEditDiff
- TOOL-02: confirmed no GSD code needs prepareArguments
- tsc --noEmit exits 0 on gsd-agent-core and gsd-agent-modes
- Test failure count at or below 258 baseline
</success_criteria>

<output>
After completion, create `.planning/phases/08-breaking-api-migrations/08-05-SUMMARY.md`
</output>
