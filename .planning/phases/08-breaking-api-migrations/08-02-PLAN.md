---
phase: 08-breaking-api-migrations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/session-migration-trace.md
  - packages/gsd-agent-core/src/sdk.ts
  - packages/gsd-agent-modes/src/main.ts
autonomous: true
requirements: [SESS-03, MREG-01]

must_haves:
  truths:
    - "Rubber-duck trace doc exists before any session migration code is written"
    - "All `new ModelRegistry(...)` calls replaced with factory methods"
    - "session_directory handler block removed from main.ts"
  artifacts:
    - path: ".planning/session-migration-trace.md"
      provides: "Session migration decision trace per D-07"
      contains: "session_start"
    - path: "packages/gsd-agent-core/src/sdk.ts"
      provides: "ModelRegistry.create() factory call"
    - path: "packages/gsd-agent-modes/src/main.ts"
      provides: "ModelRegistry.create() factory + no session_directory handler"
  key_links:
    - from: "packages/gsd-agent-core/src/sdk.ts"
      to: "packages/pi-coding-agent/src/core/model-registry.ts"
      via: "ModelRegistry.create() static factory"
      pattern: "ModelRegistry\\.create\\("
    - from: "packages/gsd-agent-modes/src/main.ts"
      to: "packages/pi-coding-agent/src/core/model-registry.ts"
      via: "ModelRegistry.create() static factory"
      pattern: "ModelRegistry\\.create\\("
---

<objective>
Create the rubber-duck session migration trace document, replace ModelRegistry constructor calls with factory methods, and remove the session_directory handler block.

Purpose: Satisfy D-07 prerequisite (trace doc before code), eliminate `new ModelRegistry` compile errors, and remove dead session_directory code.
Output: Trace doc at `.planning/session-migration-trace.md`, factory calls in sdk.ts and main.ts, session_directory block removed from main.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-breaking-api-migrations/08-CONTEXT.md
@.planning/phases/08-breaking-api-migrations/08-RESEARCH.md
@.planning/phases/08-breaking-api-migrations/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rubber-duck session migration trace document</name>
  <files>.planning/session-migration-trace.md</files>
  <read_first>
    - .planning/phases/08-breaking-api-migrations/08-CONTEXT.md (D-05, D-06, D-07, D-08 — the three reason-value mappings, extension author migration note, SessionBeforeSwitchEvent note)
    - packages/pi-coding-agent/src/core/extensions/types.ts (SessionStartEvent at line ~445 — exact reason values and field shape)
  </read_first>
  <action>
Per D-07: Create `.planning/session-migration-trace.md` containing three required sections:

**Section 1 — Reason-value mappings (from D-05):**
| Old Event | Old Reason | New Event | New Reason |
|-----------|------------|-----------|------------|
| `session_switch` | `"new"` | `session_start` | `"new"` |
| `session_switch` | `"resume"` | `session_start` | `"resume"` |
| `session_fork` | (none) | `session_start` | `"fork"` |

**Section 2 — Extension author migration note (from D-06):**
- `on("session_switch", handler)` becomes `on("session_start", handler)` with a reason guard
- Handlers must check `event.reason` to avoid running on `"startup"` or `"reload"` events
- Example: `if (event.reason !== "new" && event.reason !== "resume") return;`
- `previousSessionFile` field is available on `SessionStartEvent` (no field migration needed per D-08)

**Section 3 — Why SessionBeforeSwitchEvent is NOT used for post-switch teardown (from D-07):**
- `SessionBeforeSwitchEvent` (type: `"session_before_switch"`) fires BEFORE the transition — used for pre-switch cancellation/cleanup
- Post-switch cleanup runs in `session_start` handlers, not `session_before_switch` handlers
- The existing `bg-shell-lifecycle.ts` cleanup reads `previousSessionFile` which is on `SessionStartEvent`, not `SessionBeforeSwitchEvent`
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && test -f .planning/session-migration-trace.md && grep -c "session_start" .planning/session-migration-trace.md && grep -c "session_before_switch" .planning/session-migration-trace.md && grep -c "previousSessionFile" .planning/session-migration-trace.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/session-migration-trace.md` exists
    - File contains all three reason mappings: `"new"`, `"resume"`, `"fork"`
    - File contains extension author migration note with reason guard example
    - File contains explanation of why `SessionBeforeSwitchEvent` is NOT used for post-switch teardown
    - `grep "previousSessionFile" .planning/session-migration-trace.md` returns at least one match
  </acceptance_criteria>
  <done>Rubber-duck trace document exists at .planning/session-migration-trace.md with all three required sections per D-07</done>
</task>

<task type="auto">
  <name>Task 2: Replace ModelRegistry constructors with factory methods and remove session_directory block</name>
  <files>packages/gsd-agent-core/src/sdk.ts, packages/gsd-agent-modes/src/main.ts</files>
  <read_first>
    - packages/gsd-agent-core/src/sdk.ts (line ~211 — `new ModelRegistry(authStorage, modelsPath)` call site, check if modelsPath can be undefined)
    - packages/gsd-agent-modes/src/main.ts (line ~405 — `new ModelRegistry(authStorage, getModelsPath())` call site; lines ~165-189 — `callSessionDirectoryHook` function; line ~202 — call site for `callSessionDirectoryHook`)
    - packages/pi-coding-agent/src/core/model-registry.ts (lines 303-309 — `ModelRegistry.create()` and `ModelRegistry.inMemory()` signatures)
  </read_first>
  <action>
Per D-10 and D-09:

**sdk.ts line ~211 (MREG-01):**
First check if `modelsPath` can be undefined by reading the surrounding code. The PATTERNS.md shows it can be undefined when `agentDir` is not provided.
- BEFORE: `const modelRegistry = options.modelRegistry ?? new ModelRegistry(authStorage, modelsPath);`
- AFTER: `const modelRegistry = options.modelRegistry ?? (modelsPath ? ModelRegistry.create(authStorage, modelsPath) : ModelRegistry.create(authStorage));`

Note: `ModelRegistry.create()` has a default for modelsJsonPath (`join(getAgentDir(), "models.json")`), so calling without the path is valid. Use `ModelRegistry.create(authStorage)` for the no-path case (NOT `ModelRegistry.inMemory`) since the session still has file persistence.

Also run `grep -rn "new ModelRegistry" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` to confirm there are exactly 2 call sites. If more are found, fix them all.

**main.ts line ~405 (MREG-01):**
- BEFORE: `const modelRegistry = new ModelRegistry(authStorage, getModelsPath());`
- AFTER: `const modelRegistry = ModelRegistry.create(authStorage, getModelsPath());`

**main.ts lines ~165-189 (SESS-03) — remove `callSessionDirectoryHook` function:**
Delete the entire `callSessionDirectoryHook` function (lines ~165-189).

**main.ts call site (~line 202) — remove `callSessionDirectoryHook` usage:**
- BEFORE:
```typescript
let effectiveSessionDir = parsed.sessionDir;
if (!effectiveSessionDir) {
  effectiveSessionDir = await callSessionDirectoryHook(extensions, cwd);
}
```
- AFTER:
```typescript
const effectiveSessionDir = parsed.sessionDir;
```

After removing `callSessionDirectoryHook`, check whether `LoadExtensionsResult` type import is still used elsewhere in the file. If only used in the deleted function's signature, remove it from the import.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/gsd-2 && grep -rn "new ModelRegistry" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/; echo "---"; grep -c "callSessionDirectoryHook" packages/gsd-agent-modes/src/main.ts; echo "---"; grep -c "session_directory" packages/gsd-agent-modes/src/main.ts; echo "---"; grep "ModelRegistry.create" packages/gsd-agent-core/src/sdk.ts packages/gsd-agent-modes/src/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "new ModelRegistry" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` returns zero matches
    - `grep "ModelRegistry.create" packages/gsd-agent-core/src/sdk.ts` returns one match
    - `grep "ModelRegistry.create" packages/gsd-agent-modes/src/main.ts` returns one match
    - `grep -c "callSessionDirectoryHook" packages/gsd-agent-modes/src/main.ts` returns `0`
    - `grep -c "session_directory" packages/gsd-agent-modes/src/main.ts` returns `0`
    - `grep "const effectiveSessionDir = parsed.sessionDir" packages/gsd-agent-modes/src/main.ts` returns one match (const, not let)
  </acceptance_criteria>
  <done>Both ModelRegistry constructor calls replaced with ModelRegistry.create(), callSessionDirectoryHook function and call site removed, session_directory fully eliminated from main.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ModelRegistry factory | AuthStorage credentials passed to factory — same trust boundary as before |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | ModelRegistry.create() default path | accept | `ModelRegistry.create()` defaults to `getAgentDir() + "models.json"` — same location as before. No new path exposure. |
| T-08-04 | Denial of Service | session_directory removal | accept | Removing the handler means extensions can no longer override session directory at startup. This is intentional — 0.67.2 removed the event type. Extensions using `session_directory` will silently not fire (no crash). |
</threat_model>

<verification>
After both tasks:
- `grep -r "new ModelRegistry" packages/gsd-agent-core/src/ packages/gsd-agent-modes/src/` returns zero matches
- `grep -r "session_directory" packages/gsd-agent-modes/src/` returns zero matches
- `.planning/session-migration-trace.md` exists with all required sections
</verification>

<success_criteria>
- Rubber-duck trace doc exists at `.planning/session-migration-trace.md` with 3 sections (reason mappings, extension author note, SessionBeforeSwitchEvent rationale)
- Zero `new ModelRegistry` in gsd-agent-core and gsd-agent-modes
- Zero `session_directory` references in gsd-agent-modes
- Zero `callSessionDirectoryHook` references in gsd-agent-modes
</success_criteria>

<output>
After completion, create `.planning/phases/08-breaking-api-migrations/08-02-SUMMARY.md`
</output>
