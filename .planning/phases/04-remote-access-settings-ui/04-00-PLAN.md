---
phase: 04-remote-access-settings-ui
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/web/remote-access-api.test.ts
  - src/web/remote-access-panel.test.ts
  - src/web/tailscale-status.test.ts
  - src/web/tailscale-setup.test.ts
autonomous: true
requirements: [SETT-01, SETT-02, SETT-03, SETT-04, TAIL-09, TAIL-10, TAIL-11]

must_haves:
  truths:
    - "All four test stub files exist and can be run with npx tsx --test without crashing"
    - "Each test file contains at least one placeholder test that passes"
  artifacts:
    - path: "src/web/remote-access-api.test.ts"
      provides: "Test stubs for password change API and Tailscale status API"
      contains: "describe.*password"
    - path: "src/web/remote-access-panel.test.ts"
      provides: "Test stubs for RemoteAccessPanel UI behaviors"
      contains: "describe.*remote"
    - path: "src/web/tailscale-status.test.ts"
      provides: "Test stubs for Tailscale status parsing and DNS name handling"
      contains: "describe.*status"
    - path: "src/web/tailscale-setup.test.ts"
      provides: "Test stubs for setup assistant logic (platform detect, install command, auth URL)"
      contains: "describe.*setup"
  key_links: []
---

<objective>
Create Wave 0 test stub files for all Phase 4 test targets declared in VALIDATION.md.

Purpose: Plans 01, 02, and 03 reference these test files in their `<verify>` commands. The stubs must exist before any Wave 1 task runs so automated verification works from the first task.
Output: Four test stub files with placeholder tests that pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-remote-access-settings-ui/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create four test stub files for Phase 4 validation targets</name>
  <files>src/web/remote-access-api.test.ts, src/web/remote-access-panel.test.ts, src/web/tailscale-status.test.ts, src/web/tailscale-setup.test.ts</files>
  <read_first>
    - .planning/phases/04-remote-access-settings-ui/04-VALIDATION.md (Wave 0 Requirements section — lists the four files and their coverage targets)
    - src/web/web-session-auth.ts (function signatures to stub against: hashPassword, rotateSessionSecret, createSessionToken)
    - src/web/tailscale.ts (function signatures to stub against: getTailscaleStatus, isTailscaleInstalled, getInstallCommand, parseTailscaleStatus)
  </read_first>
  <action>
    Create four test stub files using `node:test` and `node:assert/strict`. Each file should import `describe`, `it` from `node:test` and `assert` from `node:assert/strict`. Each `it()` block should contain `assert.ok(true, 'placeholder')` so the tests pass immediately.

    **1. `src/web/remote-access-api.test.ts`** (covers SETT-01, SETT-02):
    ```
    describe('Password change API', () => {
      it('should hash and store new password', ...)
      it('should reject passwords shorter than 4 chars', ...)
      it('should rotate session secret after password change', ...)
      it('should re-issue session cookie for current browser', ...)
    })
    describe('Tailscale status API', () => {
      it('should return connected status with hostname and URL', ...)
      it('should return connected: false when Tailscale is not running', ...)
      it('should return installed: false when Tailscale CLI is missing', ...)
    })
    ```

    **2. `src/web/remote-access-panel.test.ts`** (covers SETT-03, SETT-04, SETT-05):
    ```
    describe('RemoteAccessPanel', () => {
      it('should show password form with min 4 char validation', ...)
      it('should display Tailscale URL as copyable link when connected', ...)
      it('should show password-required notice when no password configured', ...)
      it('should launch setup assistant on button click', ...)
    })
    ```

    **3. `src/web/tailscale-status.test.ts`** (covers SETT-02, TAIL-11, SETT-04):
    ```
    describe('Tailscale status parsing', () => {
      it('should strip trailing dot from DNSName', ...)
      it('should build valid HTTPS URL from hostname', ...)
      it('should return null when tailscale is not connected', ...)
      it('should parse hostname from status JSON', ...)
    })
    ```

    **4. `src/web/tailscale-setup.test.ts`** (covers SETT-03, TAIL-09, TAIL-10):
    ```
    describe('Tailscale setup assistant', () => {
      it('should detect platform via process.platform', ...)
      it('should return brew install command for darwin', ...)
      it('should return curl script command for linux', ...)
      it('should parse auth URL from tailscale up stderr', ...)
      it('should report success when tailscale up exits 0', ...)
    })
    ```

    All tests are placeholder stubs (pass immediately). They will be filled with real assertions when Plans 01, 02, and 03 implement the production code.
  </action>
  <verify>
    <automated>npx tsx --test src/web/remote-access-api.test.ts src/web/remote-access-panel.test.ts src/web/tailscale-status.test.ts src/web/tailscale-setup.test.ts</automated>
  </verify>
  <done>All four test stub files exist, import node:test and node:assert/strict, contain describe/it blocks matching VALIDATION.md targets, and pass when run with npx tsx --test.</done>
</task>

</tasks>

<verification>
- `ls src/web/remote-access-api.test.ts src/web/remote-access-panel.test.ts src/web/tailscale-status.test.ts src/web/tailscale-setup.test.ts` lists all four files
- `npx tsx --test src/web/remote-access-api.test.ts src/web/remote-access-panel.test.ts src/web/tailscale-status.test.ts src/web/tailscale-setup.test.ts` exits 0
</verification>

<success_criteria>
- Four test stub files exist at the paths declared in VALIDATION.md Wave 0 Requirements
- All stubs pass when run with npx tsx --test
- Each stub contains describe/it blocks for the requirements it covers
</success_criteria>

<output>
After completion, create `.planning/phases/04-remote-access-settings-ui/04-00-SUMMARY.md`
</output>
